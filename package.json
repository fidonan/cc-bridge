{
  "name": "cc-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local bridge for sustained collaboration between two Claude Code windows",
  "type": "module",
  "main": "src/bridge.ts",
  "scripts": {
    "start": "bun run src/bridge.ts",
    "test": "bun test src",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && bun test src"
  },
  "keywords": [
    "claude-code",
    "mcp",
    "agent",
    "bridge",
    "multi-agent",
    "dual-claude",
    "cc-switch"
  ],
  "author": "fido",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
